
export default class Chart {
    constructor(type, labels, datasets, title) {
        this.type = type;
        this.labels = labels;
        this.datasets = datasets;
        this.title = title;
    }
}
